# Leçons — Produits locaux

Fichier mis à jour automatiquement après chaque correction de Benjamin.
Format : [YYYY-MM-DD] | ce qui s'est mal passé | règle à suivre la prochaine fois

---

<!-- Entrées ajoutées ici, les plus récentes en bas -->
[2026-05-27] | matché des labels sur TOUS les producteurs d'une commune (RICHARD LAMBERT taggé cducentre car GAEC Aupetit Dubois est dans la même commune Chaillac) — confusion entre "il y a une fiche label dans la commune" et "CE producteur a le label" | un label se rattache à UN producteur précis : matcher item label → producteur SIRENE par SIRET, ou par (même commune ET token distinctif commun ≥4 char hors mots génériques comme Ferme/GAEC/Saint). Jamais tagger toute une commune. Un item label sans commune ET sans SIRET n'est PAS matchable → le skip.
[2026-05-27] | activé une source scrapée (Visit Limousin) dont la liste n'affiche ni la commune ni un nom propre (texte pollué par dates/descriptions) → faux positifs en cascade | avant d'activer une source label, vérifier que le scrape produit un NOM propre ET une COMMUNE. Si la commune n'est que sur les fiches détaillées (pas dans la liste), la source est inexploitable sans crawl par fiche → la désactiver plutôt que générer du bruit.
[2026-05-27] | oublié le label "© du Centre" (marque régionale Centre-Val de Loire) dans la liste des labels à scraper, alors qu'il était apparu deux fois dans mes propres résultats de recherche (Croquets de Charost, huile L'originale via cducentre.com) | quand un domaine ou une marque apparaît plusieurs fois dans les résultats de recherche pendant une session, le repérer comme signal et le considérer systématiquement pour les listes de labels/sources, surtout pour les marques régionales (© du Centre, Berry Province, Saveurs du Limousin, Marque Parc, etc.)
[2026-05-27] | laissé un import `from streamlit_folium import st_folium` dans app.py marqué "noqa F401" alors que la lib n'était pas dans requirements.txt — crash ModuleNotFoundError au lancement | ne JAMAIS laisser d'import non utilisé "au cas où" ; si une lib n'est pas dans requirements.txt ET pas utilisée dans le code, virer l'import sec. Vérifier mentalement chaque ligne `import` avant Write d'un fichier Python : (1) la lib est-elle dans requirements ? (2) est-elle réellement utilisée plus bas ?
[2026-05-27] | construit une clé de cache concaténant TOUS les codes NAF + départements → nom de fichier de 300+ caractères, dépasse MAX_PATH Windows (260) → FileNotFoundError au write | Benjamin est sur Windows. Toujours hasher/tronquer les noms de fichiers générés dynamiquement quand ils dépendent d'une liste : préfixe lisible (≤40 char) + hash court (md5[:12]). Plafond NAME_MAX = 80 caractères par sécurité.
[2026-05-27] | recopié l'URL "jveuxdulocal-bfc.fr" avec un tiret alors que Benjamin avait écrit "jveuxdulocal bfc.fr" sans tiret, en l'embellissant "façon normalisée" — la vraie URL est `jveuxdulocalbfc.fr` (sans tiret) | quand Benjamin fournit une URL, la prendre EXACTEMENT comme il l'écrit, sans "normaliser" ni ajouter de tirets/séparateurs présumés. URL = chaîne littérale à respecter caractère par caractère. Si doute, lui demander, ne pas deviner.
[2026-05-27] | listé 13 régions avec URLs sans tester aucune — résultat : 9 sur 29 URLs erronées (cantal-destination avec tiret, maregionsesterroirs sans tiret, vendee-qualite.com pas .fr, aria-nouvelle-aquitaine.com pas aria-na.fr, terresduvar sans tirets et en HTTP uniquement, etc.) | AVANT de finaliser un ensemble d'URLs : tester avec curl -L --max-time 10 pour vérifier le code HTTP de chacune. Garder seulement celles qui répondent 200. Pour celles qui répondent 000, chercher la bonne URL via WebSearch ou retirer la source.
[2026-05-27] | configuré les URLs label sur les pages d'accueil (`fabrique-en-aveyron.fr/` au lieu de `/entreprises/`, `maregionsesterroirs.fr/` au lieu de `/producteur/`...) → scraper extrait 1-7 items au lieu de 100+ membres réels | quand on cible une marque ombrelle, l'URL doit pointer vers la PAGE D'ANNUAIRE STRUCTURÉE, pas la page d'accueil marketing. Tester systématiquement les variantes courantes : /adherents/, /entreprises/, /producteurs/, /membres/, /producteur/, /nos-membres/, /nos-adherents/. Vérifier la taille HTML : page d'accueil ~30KB / annuaire riche 100KB+.
